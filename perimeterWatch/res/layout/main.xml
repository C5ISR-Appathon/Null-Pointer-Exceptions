<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp" >

    <TextView
        android:id="@+id/name"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:layout_centerHorizontal="true"
        android:hint="Welcome to Perimeter Watch" />

    <Button
        android:id="@+id/serverselect"
        android:layout_width="96dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/quit"
        android:layout_alignLeft="@+id/name"
        android:layout_below="@id/name"
        android:layout_toLeftOf="@+id/quit"
        android:text="Server Mode" />

    <Button
        android:id="@+id/quit"
        android:layout_width="96dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/clientselect"
        android:layout_centerHorizontal="true"
        android:text="Quit" />
    
    <Button
        android:id="@+id/camera"
        android:layout_width="96dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/quit"
        android:layout_centerHorizontal="true"
        android:text="Camera" />

    <Button
        android:id="@+id/clientselect"
        android:layout_width="146dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/name"
        android:layout_toRightOf="@+id/quit"
        android:text="Client Mode" />

</RelativeLayout>